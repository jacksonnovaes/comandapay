package com.codexmind.establishment.usecases.pix;

import com.codexmind.establishment.domain.PixTransaction;
import com.codexmind.establishment.dto.PayloadPixDTO;
import com.codexmind.establishment.dto.TransactionDTO;

public class DoPaymentAsaasImpl implements DoPaymentInterface{

    @Override
    public PixTransaction pixPaymentEfi(TransactionDTO transactionDTO) {
        return null;
    }

    @Override
    public PixTransaction pixPayment(PayloadPixDTO payloadPixDTO) {
        return null;
    }
}
